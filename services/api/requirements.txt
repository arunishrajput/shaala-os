# Pinned per PROMPT.md §3. Do not bump mid-build (version drift is the #1 way an
# AI-assisted build dies). Versions verified against PyPI at Phase 1 kickoff.

fastapi==0.141.1
uvicorn[standard]==0.52.1
sqlalchemy==2.0.51
alembic==1.19.0
psycopg[binary]==3.3.4
pydantic==2.13.4
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
# Pinned <4.1: passlib 1.7.4's backend self-test breaks on bcrypt's stricter
# 72-byte check introduced in 4.1 ("password cannot be longer than 72 bytes"
# raised during passlib's own internal detect_wrap_bug probe, not real input).
bcrypt==4.0.1
python-multipart==0.0.32

# The solver — never a greedy/random scheduler.
ortools==9.15.6755

# Headless build only; the GUI build breaks in Docker (PROMPT.md §3).
opencv-python-headless==5.0.0.93

pillow==12.3.0
reportlab==5.0.0
qrcode==8.2
httpx==0.28.1
apscheduler==3.11.3
